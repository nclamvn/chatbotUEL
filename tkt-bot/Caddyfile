# SITE_ADDRESS: domain production (vd tkt.uel.example), compose cấp mặc định :80.
{$SITE_ADDRESS} {
	handle /api/* {
		uri strip_prefix /api
		reverse_proxy api:8000
	}

	handle {
		reverse_proxy web:3000
	}
}
